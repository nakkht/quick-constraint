os: osx
osx_image: xcode11.3
language: swift

env:
  global:
    - LC_CTYPE=en_US.UTF-8
    - LANG=en_US.UTF-8

branches:
  only:
  - master
  - develop

before_install:
  - gem install xcpretty --no-document --quiet
  - xcpretty --version

jobs:
  include:
    - stage: Build
      script:
        - xcodebuild -project QuickConstraint.xcodeproj -scheme QuickConstraint -destination 'platform=iOS Simulator,name=iPhone 5s,OS=10.3.1' clean build | xcpretty -c
        - xcodebuild -project QuickConstraint.xcodeproj -scheme QuickConstraint -destination 'platform=iOS Simulator,name=iPhone 11 Pro,OS=latest' clean build | xcpretty -c

    - stage: Test
      script:
        - xcodebuild -project QuickConstraint.xcodeproj -scheme QuickConstraint -destination 'platform=iOS Simulator,name=iPhone 5s,OS=10.3.1' test | xcpretty -c
        - xcodebuild -project QuickConstraint.xcodeproj -scheme QuickConstraint -destination 'platform=iOS Simulator,name=iPhone 11 Pro,OS=latest' test | xcpretty -c

after_success:
  - bash <(curl -s https://codecov.io/bash) -J 'Logr'
